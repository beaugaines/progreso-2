<h1>Project Portfolio</h1>

<div class="row">
 <div class="col-md-8">
   <%= link_to 'Add Project', new_project_path, id: "new_link" %>
   <div class="projects">
   <% @projects.each do |project| %>   
       <div><%= link_to project.title, project %> </div>
   <% end %>

   </div>
 </div>
</div>